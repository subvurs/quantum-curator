================================================================================
QUANTUM INTEL → QUANTUM CURATOR MIGRATION PLAN
================================================================================

  Author:     Mark Eatherly + Claude (claude-opus-4-7)
  Started:    June 9, 2026
  Status:     Phase 4 complete; Phase 5 complete (a/c/d/e/f code +
              docs; 5g doc closure landed Jun 9). Synthesizer reads
              today_curated_seeds (curated_at-filtered) crossed with
              the 1216-row frozen historical inventory; deliver()
              routes citations to the right stamp table via
              SEED_ID_OFFSET; daily_summary dual-sourced; CLI gap
              closed across 4 call sites + emailer fallback;
              tests/test_intel_synth.py pins routing +
              channel-separation; 26/26 tests pass.
              Hand-run against Jun 9 09:41 UTC GH Actions cache: 5
              non-trivial briefs from 45 curated_posts, 14 unique
              stamps, 0 cross-namespace collisions, brief confidence
              0.58-0.72.
              Phase 5 commits remain unpushed per Mark's "Hold off
              until Phase 5 is done" standing order. Phase 4c
              (Intel launchd retirement) is blocked on ≥ 3 days of
              non-empty Intel output, which starts after the push.
              Two minor deployment items close automatically on push:
              ensure_schema() applies the 5c ALTER TABLE; CLI source
              flip stops the "0 briefs / quiet day" output.
  Owner:      Mark Eatherly
  Doc path:   /Users/mvm/Desktop/quantum-curator/INTEL_TO_CURATOR_MIGRATION.txt

  This document is the single source of truth for the migration. It is
  updated at the end of every phase. Do not let the work jump phases
  without amending this file first.

--------------------------------------------------------------------------------
1. WHAT WE ARE DOING
--------------------------------------------------------------------------------

Migrating the Quantum Intel synthesis pipeline (today living at
~/Library/Application Support/quantum_intel/) into the Quantum Curator
backend (~/Desktop/quantum-curator/) as a post-processing step that
runs after Curator's existing daily intake.

After migration:
  - Curator runs once per day as today (intake + curate + post to
    Quantum Crier + Qrater + email + per-article Bluesky)
  - As a NEW last step, Curator runs the (re-implemented) Intel
    synthesis pipeline over its own articles + the migrated history
  - Intel sends ITS OWN email (separate SMTP, separate body) with
    daily briefs + day-over-day summary
  - Curator's email gets a NEW subvurs_impact_score section
  - Curator posts a NEW daily Bluesky summary (less detailed than email,
    links to qrater.org)
  - Intel's standalone launchd cron is retired
  - Intel's briefs/ directory is preserved as a read-only historical
    archive

--------------------------------------------------------------------------------
2. WHY
--------------------------------------------------------------------------------

The Intel pipeline has stalled chronically since at least April 2026.
Documented multi-hour hangs from review.log + run_log.json:

  - Apr 24 2026  review file not created (claude rc=0)
  - May 2  2026  review file not created (claude rc=1)
  - May 16-28    multiple rc=1 review failures
  - May 20 2026  single doc consumed 4h35m wall clock (catalog deadline
                 exception was being swallowed by chat_with_retry's
                 except RuntimeError, sleeping, then re-attempting)
  - May 21 2026  1h18m stall in collectors (DuckDuckGo CAPTCHA scrape)
  - Jun 7  2026  5h25m hang in SMTP (no socket timeout)
  - Jun 8  2026  synthesize 6h 8m, collect 57m, traced to PEP 475
                 EINTR-restart silently defeating signal.SIGALRM
                 contextmanagers in synthesizer/cataloger/collectors

Root causes consolidated into four classes:

  (a) PEP 475 EINTR auto-retry defeating SIGALRM-based deadlines.
      The alarm fires, the handler runs, but the very next syscall
      picks up where it left off (recv() inside resp.read(), or
      Popen.communicate() pipe reads) and the deadline does nothing.
  (b) llm_client falling back to Ollama Cloud when ANTHROPIC_API_KEY
      and NVIDIA_API_KEY are missing from the launchd environment.
      Ollama Cloud slow-drips bytes on a 600s urlopen timeout — the
      socket never goes idle, the per-socket timeout never fires.
  (c) Multi-provider fallback chain (Anthropic → NIM → Ollama local →
      Ollama Cloud) compounding any single provider's failure into
      cascading retries.
  (d) Duplicate scoring infrastructure: Intel has its own (recently
      added, Jun 2026) Stage 2.5 subvurs_impact scorer that fails
      closed when ANTHROPIC_API_KEY isn't set. Curator ALREADY has
      working Subvurs-impact scoring (loads .env via pydantic-settings,
      writes to curated_posts.subvurs_impact_score).

Migrating into Curator escapes all four:

  (a) Curator is async-native (httpx, not urllib). No PEP 475 issue.
  (b) Curator's pydantic-settings loads .env at startup — keys are
      reliably present.
  (c) Curator already uses the Anthropic SDK directly. No fallback
      chain to compound.
  (d) Move scoring to a single place that already works.

--------------------------------------------------------------------------------
3. PRESERVATION GUARANTEES (non-negotiable per Mark, Jun 9 2026)
--------------------------------------------------------------------------------

The migration MUST preserve:
  - All 1216 cataloged inventory entries (entry_ids 0..1215)
  - All 38 dedup-only fingerprint sentinels
  - The 9 Stage 2.5-scored entries' subvurs_impact_* fields
  - entry_id continuity (next synth call resumes at 1216)
  - Intel's briefs/ directory (kept read-only, no rewrite)

Already done in Phase 1 (see §5 below). One-time backup taken:
  ~/Library/Application Support/quantum_intel/inventory.json.preimport.bak.20260609_023717

--------------------------------------------------------------------------------
4. ARCHITECTURE — BEFORE vs AFTER
--------------------------------------------------------------------------------

BEFORE (today)

  Curator (cron via .github/workflows/daily-curator.yml)
    └── intake (arXiv quant-ph, ~17 RSS, NewsAPI if key)
    └── curate (LLM commentary + Phase A subvurs_notes string +
                Phase B subvurs_impact_score on curated_posts)
    └── publish (Quantum Crier site + Qrater site)
    └── email (insights_report — subvurs_notes only)
    └── Bluesky (per-article share)

  Intel (launchd cron, separate venv, separate .env)
    └── COLLECT (curator feed @ qrater.org, arXiv quant-ph + cond-mat,
                 DDG Lite 13 queries)
    └── CATALOG (LLM structured extraction, SHA-256 dedup)
    └── SUBVURS_SCORE (Stage 2.5, recently added, ~0.7% coverage so far)
    └── SYNTHESIZE (combinatorial-product brief generation)
    └── DELIVER (writes .md briefs to ~/Library/.../briefs/)
    └── SCAFFOLD (off by default since v1.6)
    └── EMAIL (SMTP brief delivery to Mark)
    └── REVIEW (separate claude CLI subprocess, frequently rc=1)

AFTER (target — REVISED Jun 9 2026 per Phase 5 plan)

  Curator (cron, same workflow file, ONE workflow)
    └── intake (arXiv quant-ph + cond-mat, ~18 RSS + Quantinuum + PRX
                Quantum, NewsAPI if key)  → raw_articles
    └── curate (unchanged — LLM commentary + Phase A + Phase B scoring)
                                          → curated_posts
    └── publish (Quantum Crier + Qrater — unchanged)
    └── email (insights_report + NEW subvurs_impact_score section)
    └── Bluesky (per-article share + NEW daily summary post linking
                 qrater.org — SHORT public rundown, distinct from
                 Intel email)
    └── INTEL SYNTH (new last step — Plan B sourcing)
        ├── TODAY'S SEEDS: pull from curated_posts where
        │   curated_at ≥ now − 1d (Curator's own intake; the same
        │   articles that just went to Quantum Crier).
        │   GOTCHA: filter on curated_at (when Curator created the
        │   post), NOT published_at (article's original date — can
        │   be months old for older arXiv papers that Curator just
        │   surfaced). See §5 (5a) for implementation; date_published
        │   is still passed to the LLM prompt so the article's real
        │   pub date is visible, but the "today" window is curator-
        │   activity-driven.
        ├── HISTORICAL CO-SOURCE: pull from quantum_intel_entries
        │   (1216 historical, frozen at Phase 1d import). These are
        │   NOT background — they are paired combinatorially with
        │   today's seeds. 5b shim subsumed by 5a; future entries do
        │   not accrete here, only via curated_posts.
        ├── combinatorial synthesis over (today × historical) pairs;
        │   anti-recurrence via recent brief citations (unchanged
        │   semantics — first_brief_at on quantum_intel_entries,
        │   plus parallel tracking on curated_posts.intel_first_brief_at)
        ├── daily summary (TL;DR + implications-vs-prior + attention
        │   recommendations, Goodhart-guarded; reads BOTH sources)
        ├── SEPARATE Intel email (own SMTP body — detailed internal
        │   review, distinct from Curator insights email AND distinct
        │   from Bluesky daily summary post)
        └── (no scaffold, no review — both dropped per Mark Jun 9 2026)

  Output channel separation (clarification Jun 9 2026):
    - Curator insights email = subvurs_notes + top-N subvurs_impact
                               table (on curated_posts only)
    - Intel email             = full brief content from synthesis
                                (curated_posts × quantum_intel_entries)
    - Bluesky daily summary   = short public rundown
                                (links → qrater.org)
    Three distinct outputs, three different shapes, three different
    audiences. None of them duplicate each other's content.

  Intel (~/Library/Application Support/quantum_intel/)
    └── launchd cron disabled
    └── inventory.json kept frozen as historical artifact (already
        backed up Jun 9 2026)
    └── briefs/ kept read-only
    └── COLLECT / CATALOG / SUBVURS_SCORE / SYNTHESIZE / DELIVER /
        EMAIL — all deprecated; module files left in place but unused
    └── pipeline_deadline.py retained (good engineering reference)

--------------------------------------------------------------------------------
5. PHASES
--------------------------------------------------------------------------------

  [DONE]  Phase 0 — SIGALRM patches (pre-migration triage)
              File added:    ~/Library/Application Support/quantum_intel/
                             pipeline_deadline.py
              Files patched: synthesizer.py, cataloger.py, collectors.py
              Helper:        run_with_deadline(seconds, fn, ...)
                             daemon thread + Thread.join(timeout=)
                             EINTR-immune (uses condition variable)
              Rationale:     keeps Intel from continuing to stall during
                             the build-out of the Curator-side replacement
              Committed:     Jun 9 2026 (see §8)

  [DONE]  Phase 1a — Curator db.py schema additions
              Tables added: quantum_intel_entries, quantum_intel_dedup,
                            bluesky_daily_summaries
              Indexes:      fingerprint, date_collected,
                            subvurs_impact_score, summary_date
              Idempotent:   CREATE IF NOT EXISTS only

  [DONE]  Phase 1b — Source additions to BUILTIN_SOURCES
              Added:
                * arXiv cond-mat (Intel had this; Curator didn't —
                  5.8% of Intel pulls verified post-import)
                * Quantinuum Blog RSS (Intel scraped via DDG; not in
                  Curator's vendor blog set)
                * PRX Quantum RSS (highest-signal APS journal; PRL
                  deliberately skipped for noise reasons)
              register_builtin_sources() is already idempotent
              (updates existing by name)

  [DONE]  Phase 1c — One-time inventory.json importer
              Module: quantum_curator/intel/import_inventory.py
              Behavior:
                * Backs up inventory.json to inventory.json.preimport.bak.<TS>
                * INSERT OR IGNORE — re-runs are no-ops
                * Folds the 9 Stage 2.5-scored entries' fields into
                  subvurs_impact_score + subvurs_impact_report JSON
                * Verifies row counts and max(entry_id) against source
              CLI: python -m quantum_curator.intel.import_inventory [--dry-run]

  [DONE]  Phase 1d — Run the importer
              Live import Jun 9 2026 02:37:17 UTC.
              Result:
                * entries_inserted:        1216 / 1216
                * dedup_inserted:          38 / 38
                * scored_carried:          9
                * max(entry_id) preserved: 1215
                * Idempotence verified:    re-run skipped 1216 + 38

  [DONE]  Phase 2 — Curator-side intel synthesizer
              Files added under quantum_curator/intel/:
                * inventory_view.py   — SQLite read-side, mirrors
                                        inventory.json shape; JSON-decodes
                                        enabling_capabilities + domain_tags
                * brief_history.py    — disk-scan anti-recurrence counter
                                        (Intel-original semantics preserved)
                * synthesizer.py      — combinatorial-product synthesis;
                                        Anthropic SDK direct (max_retries=2),
                                        SQLite-backed inventory,
                                        DEFAULT_MODEL = claude-sonnet-4-5,
                                        fail-closed on LLM failure / non-JSON
                * daily_summary.py    — TL;DR + implications-vs-prior +
                                        attention recs; FORBIDDEN_PHRASES
                                        soft-mask, fail-closed on missing
                                        required keys, deterministic
                                        no-new-content payload (no LLM call)
                * emailer.py          — separate SMTP_SSL with 30s socket
                                        timeout (Jun 7 SMTP hang fix), HTML
                                        aesthetic mirrors legacy Intel email,
                                        uses Curator's settings.smtp_email /
                                        smtp_app_password
              CLI wiring (cli.py):
                * synthesize-intel  [--days N] [--max-briefs N] [--model M]
                                     [--dry-run]
                * intel-summary     [--days N] [--prior-days N]
                                     [--format text|bluesky|json]
                * intel-email       [--days N] [--no-synth] [--max-briefs N]
                                     [--dry-run]
              Anti-recurrence: brief_history.recent_brief_citations()
                + inventory_view.mark_first_brief_at() stamp on delivery.
              Briefs land in settings.data_dir / "intel_briefs".
              Smoke-tested Jun 9 2026: imports clean, CLI registers all 3
              commands, inventory_view.load_inventory() → 1216 rows,
              today_entries(days=7) → 86 rows.

  [DONE]  Phase 3 — Bluesky daily summary + Curator email scoring
              (3a) bluesky.py — added:
                   * BlueskySharer.share_daily_summary(text, link,
                     summary_date=None) — idempotent via UNIQUE
                     summary_date; auto-truncates at 300 chars; appends
                     link if missing; adds link facet via byte offset.
                   * record_daily_summary_share(summary_date, uri, cid,
                     post_text) — INSERT OR REPLACE keyed by date.
                   * is_daily_summary_shared(summary_date) — UTC YYYY-MM-DD.
              (3b) email_report.py — added IMPACT_TABLE_HEADER + top-N
                   block (IMPACT_TOP_N=5, IMPACT_MIN_SCORE=0.05 floor).
                   Section auto-suppressed when no posts meet the
                   threshold, so pre-Phase-B emails are unchanged.
                   Header line now shows "{connections} connections ·
                   {impact} scored".
              (3c) cli.py — added `share-intel-summary` command:
                   builds daily_summary, renders via render_bluesky,
                   posts via BlueskySharer.share_daily_summary.
                   Honors --dry-run; skips if already shared today.
              Smoke-tested Jun 9 2026:
                * imports clean (bluesky + email_report)
                * share-intel-summary --help registers
                * build_insights_report renders w/ 0 scored (no posts
                  scored yet in DB)
                * synthetic-post test: 0.87 + 0.42 included, 0.02
                  excluded by floor, header shows "2 scored"

  [DONE]  Phase 4a — Workflow wiring (daily-curator.yml)
              File patched: .github/workflows/daily-curator.yml
              Added two steps after "Share to Bluesky" and before
              "Email Subvurs insights report":

                - name: Send Intel daily email
                  continue-on-error: true
                  run: quantum-curator intel-email --days 1 --max-briefs 5

                - name: Share Intel summary to Bluesky
                  continue-on-error: true
                  run: quantum-curator share-intel-summary --days 1
                       --link https://qrater.org

              Design decisions:
                * No separate `synthesize-intel` step. intel-email runs
                  synth + summary + send in one pass; the briefs list
                  inside intel-email is in-process — splitting it would
                  require either disk-handoff (added wiring) or
                  --no-synth (which leaves the email's briefs section
                  empty). One LLM call per day either way.
                * continue-on-error: true on both Intel steps so a
                  single LLM hiccup can't take down the rest of the
                  daily run (publish + Bluesky + insights email all
                  precede these and are insulated).
                * share-intel-summary is idempotent on summary_date
                  (UNIQUE constraint on bluesky_daily_summaries) — safe
                  for same-UTC-day re-runs.
              YAML validated (yaml.safe_load): 20 named steps, correct
              order, no syntax errors.

  [DONE]  Phase 4b — Parallel-run protocol (operational, no code)
              Goal: confirm Curator-side Intel matches legacy-Intel
              output (briefs, emails, Bluesky post) for one full week
              before unloading Intel's launchd plists.

              Procedure:
                1. Leave both Intel launchd plists loaded:
                     com.subvurs.quantum-intel.plist
                     com.subvurs.quantum-intel-review.plist
                   These read ITS OWN inventory.json — they do not
                   write to Curator's DB. Non-overlapping. (See R3.)
                2. Daily Curator workflow now runs intel-email +
                   share-intel-summary on its own schedule (06:00 UTC).
                3. Each day during the window, compare:
                     - Intel's own email vs Curator's new intel-email
                       (subject line + brief count + brief titles).
                     - Bluesky daily post landed once and only once;
                       record in `bluesky_daily_summaries`.
                4. Track failures in both pipelines side-by-side. If
                   Curator's intel-email rc!=0 for two days in a row,
                   roll back per §9 and investigate before continuing.
                5. After 7 consecutive days of parity (or 5 successful
                   days if Intel itself stalls during the window —
                   likely given Phase 0 history), proceed to 4c.

              Parity checklist captured in /tmp during the window —
              not committed to repo. Intel's existing inventory.json
              continues to grow during this period; Curator's
              quantum_intel_entries table does NOT receive those new
              entries (Phase 1c importer was one-shot). That gap is
              acceptable per D11 — at cutover, Intel inventory freezes
              and Curator's intake takes over (cond-mat + Quantinuum
              + PRX Quantum sources already added in Phase 1b).

  [DONE]  Phase 4c — Intel launchd retirement runbook
              Trigger: 4b parallel-run window passes per the checklist
              above.

              **Phase 5 dependency (added Jun 9 2026):** runbook is
              documented but execution is BLOCKED until Phase 5
              produces non-empty Intel briefs + daily summary
              for ≥ 3 consecutive days. First workflow_dispatch run
              (Jun 9 2026, run id 27193404135) exposed the content
              gap — intel-email and share-intel-summary fired
              successfully but read from the post-import-frozen
              quantum_intel_entries table (0 entries in last 24h),
              so they shipped "0 briefs" and "quiet day" respectively
              while Curator's insights email simultaneously found 15
              subvurs_notes connections on curated_posts. Empty
              output is not parity. See Phase 5 below.

              Retirement steps (run in order; capture output for the
              changelog):

                # 1. Confirm both plists are loaded
                launchctl list | grep quantum-intel

                # 2. Unload main pipeline plist
                launchctl unload \
                  ~/Library/LaunchAgents/com.subvurs.quantum-intel.plist

                # 3. Unload review pipeline plist (per D8, review was
                #    already low-value — frequent rc=1 — but disable
                #    it explicitly for cleanliness)
                launchctl unload \
                  ~/Library/LaunchAgents/com.subvurs.quantum-intel-review.plist

                # 4. Confirm they no longer appear in launchd
                launchctl list | grep quantum-intel  # → empty

                # 5. Leave the plist files in place (gives us a clean
                #    `launchctl load` path if we ever need to rerun
                #    legacy Intel for forensic comparison). Do NOT
                #    `rm` them.

                # 6. Freeze inventory.json: no editing, no rotation.
                #    The pre-import backup at
                #    inventory.json.preimport.bak.20260609_023717
                #    stays alongside it as the canonical snapshot at
                #    cutover.

              Documentation freeze (do at the same time):
                * Append a "FROZEN — migrated to Quantum Curator" note
                  to the top of:
                    ~/Library/Application Support/quantum_intel/
                      _ROLLING_NOTES.md
                  with a pointer back to this migration doc.
                * Update CHANGELOG below with the unload timestamp
                  and the final inventory.json row count at freeze.

              Rollback (if Curator-side Intel fails post-cutover):
                # 1. Re-load Intel plists
                launchctl load \
                  ~/Library/LaunchAgents/com.subvurs.quantum-intel.plist
                launchctl load \
                  ~/Library/LaunchAgents/com.subvurs.quantum-intel-review.plist
                # 2. Disable the two Curator workflow steps by
                #    commenting them out in daily-curator.yml (same
                #    pattern as the Twitter/X disabled block already
                #    in the file).
                # 3. Intel resumes against its own inventory.json
                #    from wherever it left off.

  [DONE]  Phase 5 — Curator-side data source pivot (Plan B)

              Problem statement (verified live Jun 9 2026):
                * intel-email rendered "0 briefs today"
                * share-intel-summary posted "quiet day"
                * Same daily run: 2 articles published, 15
                  subvurs_notes connections in curated_posts
                * Root cause: synthesizer + daily_summary read from
                  quantum_intel_entries.date_collected over last N
                  days; Phase 1d was a one-shot import (max
                  date_collected = 2026-06-08); nothing populates
                  new rows day-to-day. The "+ new" arrow in §4
                  AFTER was never wired.

              Decision (Mark, Jun 9 2026): Plan B
                * NEW seeds come from Curator's own intake
                  (curated_posts) — the same articles already
                  going to Quantum Crier / Qrater / insights email
                * The 1216 historical quantum_intel_entries stay
                  ACTIVE as combinatorial co-source — paired with
                  today's seeds in the cross-product, not just
                  background. Same role they had pre-migration.
                * Email vs Bluesky output channels separated
                  explicitly: Intel email = detailed internal
                  review; Bluesky daily summary = short public
                  rundown linking qrater.org. Already two code
                  paths; Phase 5 just makes sure each pulls from
                  the right source.

              (5a) Synthesizer source pivot  [DONE - June 9, 2026]
                   File: quantum_curator/intel/synthesizer.py
                            quantum_curator/intel/inventory_view.py
                   Done:
                     * inventory_view.SEED_ID_OFFSET = 2_000_000
                       (synthetic int IDs for curated_posts seeds so
                       they cannot collide with quantum_intel_entries
                       row IDs; max import was 1215)
                     * inventory_view.today_curated_seeds(days=1)
                       projects curated_posts.status='published' into
                       InventoryEntry-shape dicts, preserving
                       _curated_post_id (UUID) for 5c routing.
                     * Date filter: curated_at (when Curator created
                       the post), NOT published_at (article's original
                       date — can be months old when Curator surfaces
                       an older arXiv paper). date_collected on the
                       seed carries curated_at so downstream "today"
                       semantics work; date_published preserves the
                       article's real pub date for the LLM prompt.
                     * synthesizer.run_intel_synthesis() pulls from
                       today_curated_seeds(days) instead of
                       today_entries(days). load_inventory() still
                       returns the 1216 historical entries, paired as
                       the inventory side of the combinatorial product
                       — exactly the role they had pre-migration.
                     * synthesizer.deliver() guards mark_first_brief_at
                       against synthetic seed IDs (>= SEED_ID_OFFSET).
                       Anti-recurrence for seed-side citations lands
                       in 5c via intel_first_brief_at on curated_posts.
                     * Empty-case log changed to "no curated_posts
                       seeds in the last Nd — nothing to synthesize"
                       so failures are diagnosable in CI logs.
                     * Tests: 11/11 pre-existing pass (no regression).
                       No new Intel-side tests yet — 5e adds coverage.
                   Worth flagging:
                     * Local dev DB is months stale (curated_at max
                       2026-03-19). All Phase 5 hand-validation must
                       happen against the GH Actions runner cache, not
                       the local DB. Pull the cache artifact via
                       `gh run download <run_id>` for 5f.

              (5b) Inventory view: add curated_posts seed helper
                   [SUBSUMED INTO 5a]
                   today_curated_seeds() was added to inventory_view.py
                   as part of 5a because the synthesizer pivot couldn't
                   land without it. Leaving the entry here for the
                   audit trail; no separate 5b commit needed.

              (5c) Schema additive: curated_posts.intel_first_brief_at
                   [DONE - June 9, 2026]
                   Done:
                     * db.py CREATE TABLE curated_posts now declares
                       `intel_first_brief_at TEXT DEFAULT NULL` with an
                       explanatory comment. Fresh DBs ship with the
                       column.
                     * db.py init_db() runs ALTER TABLE ADD COLUMN
                       inside try/except sqlite3.OperationalError for
                       existing DBs. Annotated to make clear which
                       error is being swallowed (per rigor rule §8).
                     * inventory_view.mark_curated_seed_first_brief_at(
                       curated_post_id: str, ts: str | None) — UPDATE
                       ... WHERE id = ? AND intel_first_brief_at IS
                       NULL. Returns True if row updated, False if
                       already set or nonexistent. Idempotent.
                     * synthesizer.deliver(seed_id_to_uuid=...) routes
                       citations:
                         eid <  SEED_ID_OFFSET → mark_first_brief_at
                                                 (Intel-side, by int eid)
                         eid >= SEED_ID_OFFSET → mark_curated_seed_first
                                                 _brief_at (curated-side,
                                                 by UUID lookup)
                       Hallucinated/missing UUIDs silently no-op
                       rather than stamping the wrong row.
                     * run_intel_synthesis() builds seed_id_to_uuid
                       from `today_curated_seeds()` output and threads
                       it into deliver().
                   Verified:
                     * Local migration applied cleanly (28/28 existing
                       rows initialized to NULL).
                     * Stamp on real UUID → True; re-stamp → False
                       (idempotent); stamp on nonexistent UUID → False
                       (silent no-op).
                     * Mixed-citation end-to-end test: one Intel-side
                       int eid + one synthetic seed eid + one bogus
                       hallucinated int eid → exactly the right two
                       rows stamped with matching timestamps; bogus
                       eid leaves no DB residue.
                     * 11/11 pre-existing tests still pass.

              (5d) Daily summary source pivot
                   [DONE - June 9, 2026]
                   File: quantum_curator/intel/daily_summary.py
                   Done:
                     * Module docstring expanded with "Dual data source
                       (Phase 5d, Plan B pivot)" section explaining
                       today = curated_posts vs prior = historical
                       quantum_intel_entries.
                     * build_daily_summary() signature now accepts
                       kw-only ``days: int = 1`` (sized today_curated
                       _seeds window) and ``prior_limit: int = 100``
                       (cap on historical sample). ``prior_days`` is
                       preserved as a no-op for backward-compat with
                       the three CLI callers (intel-summary, intel-
                       email, share-intel-summary) that still pass
                       it via click.option.
                     * Default new_entries = today_curated_seeds(days
                       =days). Same Plan B pivot as Phase 5a's
                       synthesizer.
                     * Default prior_entries = load_inventory()
                       [:prior_limit] minus today_ids (defensive
                       filter; today_ids >= 2_000_000 cannot collide
                       with historical max ~1215, but kept for ID-
                       space changes).
                     * SUMMARY_PROMPT updated: "# Prior 7-Day Window"
                       → "# Prior Corpus (historical context)";
                       implications bullet refers to "prior historical
                       corpus" instead of "prior 7-day window".
                     * _no_new_content_payload text updated:
                         tldr:        "No new curated posts to
                                       summarize in the last 24h."
                         implications: "Historical corpus has X
                                       entries; no shift today."
                       window dict + tags + keys unchanged (contract
                       parity per spec).
                     * FORBIDDEN_PHRASES list unchanged (per spec).
                   Verified:
                     * Signature accepts days=, prior_days=, prior
                       _limit= without TypeError.
                     * Smoke test (mocked data sources, mocked
                       Anthropic key): defaults route through today
                       _curated_seeds + load_inventory; quiet-day
                       payload renders honest curated-posts wording.
                     * All three legacy CLI kwarg shapes
                       (new_entries=[], prior_days=7) still work.
                     * 11/11 pre-existing tests still pass.
                   CLI gap (deferred, NOT in 5d scope):
                     * cli.py intel-summary / intel-email /
                       share-intel-summary all still pass
                       ``new_entries = inventory_view.today_entries
                       (days=days)`` — that's quantum_intel_entries
                       (frozen Phase 1d import → returns [] in
                       production). They bypass the new default.
                     * Until those three call sites pivot to
                       today_curated_seeds(days=days), the workflow
                       will fire the (now correctly worded) "quiet
                       day" payload every run regardless of
                       curated_posts activity.
                     * Fix is a 3-line change per call site — fold
                       into 5e CI smoke-test patch or land as a
                       standalone 5d.cli follow-up. Tracked here so
                       5f doesn't blame the daily_summary defaults
                       when the CLI is still the explicit feeder.

              (5e) [DONE - June 9, 2026] CLI source pivot + channel-
                   separation smoke tests.

                   Scope grew vs original plan: the "no code, smoke
                   test" framing assumed 5d's defaults routing would
                   feed all CLI commands automatically. In practice
                   the four CLI sites passed new_entries explicitly
                   (the gap flagged under 5d), so a smoke test would
                   have always rendered quiet-day regardless of
                   curated_posts activity. Closing the gap was
                   prerequisite to any meaningful 5e verification.

                   Code changes (all minimal — 1-line source swap +
                   seed_id_to_uuid wiring at the two deliver() sites):
                     * cli.py intel_summary       → today_curated_seeds
                     * cli.py intel_email         → today_curated_seeds;
                                                    deliver() passes
                                                    seed_id_to_uuid map
                     * cli.py share_intel_summary → today_curated_seeds
                     * cli.py synthesize_intel    → today_curated_seeds;
                                                    deliver() passes
                                                    seed_id_to_uuid map;
                                                    "no entries" message
                                                    text updated
                     * emailer.py send_intel_email fallback →
                                                    today_curated_seeds
                                                    (defensive; CLI
                                                    always feeds
                                                    new_entries
                                                    explicitly)

                   Tests added (tests/test_intel_synth.py, 15 cases):
                     1. deliver() routing:
                        * eid < SEED_ID_OFFSET → mark_first_brief_at
                        * eid >= SEED_ID_OFFSET with UUID →
                          mark_curated_seed_first_brief_at
                        * eid >= SEED_ID_OFFSET with no UUID → silent
                          no-op (hallucinated-ID case)
                        * seed_id_to_uuid=None → falls back to {} map
                        * non-integer entry_ids skipped without crash
                     2. Idempotence: second cite of same eid /
                        curated_post_id does NOT overwrite the
                        original timestamp (UPDATE ... WHERE col IS
                        NULL semantics).
                     3. today_curated_seeds() projection:
                        * synthetic entry_ids start at SEED_ID_OFFSET
                        * only status='published' rows seed
                        * curated_at window honored
                        * curator_commentary → summary,
                          summary → technical_detail,
                          entry_type = "curated_post"
                     4. Channel-separation smoke (no LLM):
                        * render_bluesky output is short (<= 280 chars),
                          contains qrater.org link, no TL;DR/
                          Implications/Worth-attention headers
                        * render_text is the multi-section digest,
                          does NOT contain the Bluesky CTA link
                        * Quiet-day _no_new_content_payload renders
                          sanely on both channels

                   Test suite: 26 passed, 19 warnings (pre-existing
                   datetime.utcnow deprecations from other modules),
                   no regressions.

                   Still out of scope for 5e:
                     * The "CI logs on a non-empty day" eyeball check
                       — that's 5f (hand-run against Jun 9 data).
                     * The Curator insights email subvurs_notes/impact
                       table check — covered by existing
                       test_curator_subvurs_impact.py (Phase B work).

              (5f) Hand-run validation against Jun 9 2026 data
                   [DONE — June 9, 2026]
                   Original spec: re-run synthesize-intel against the
                   Jun 9 post-workflow DB; expect ≥ 5 briefs, each
                   non-empty.
                   Outcome (artifact curator-db-27197625739):
                     * 5 briefs delivered, 2.7-3.0 KB each,
                       confidence 0.58-0.72, full Discoveries
                       Combined / Target Market / Risk Factors
                       structure on every one.
                     * 14 unique seeds cited across 15 citation slots
                       (one self-pair: 2000002 cited by two briefs).
                     * 14 curated_posts.intel_first_brief_at stamps
                       written; 0 quantum_intel_entries
                       .first_brief_at writes (correct routing).
                   Deviations from spec, surfaced as findings (full
                   detail in §8 Phase 5f entry):
                     * "Top-impact curated_post paired with historical
                       entries" was unattainable as written — the GH
                       Actions runner has 0 historical entries
                       (Phase 1d never ran there). Briefs landed as
                       today×today combinations (synth prompt's
                       allowed-but-exception path); the "(No prior
                       history)" fallback kicked in cleanly without
                       quality degradation.
                     * Phase 5c migration not deployed on runner
                       either; applied locally via idempotent ALTER
                       TABLE before invoking the CLI. Both gaps
                       close on push (5g push checklist).

              (5g) Migration doc + workflow notes  [DONE — June 9, 2026]
                   Closes Phase 5 on the documentation side. Actions:
                     * §4 AFTER corrected: TODAY'S SEEDS filter is
                       curated_at, not published_at. Gotcha called
                       out inline (older arXiv papers re-surfaced by
                       today's intake would otherwise be filtered out
                       entirely by published_at, or — worse —
                       silently miss the window even when Curator
                       just published commentary on them).
                     * §4 AFTER: HISTORICAL CO-SOURCE clarified —
                       quantum_intel_entries is frozen at Phase 1d;
                       no shim accretes new rows there. 5b is
                       formally subsumed.
                     * §5 Phase 4c trigger language already correct:
                       retirement fires after ≥ 3 days of non-empty
                       Intel output, NOT on parallel-run wall time.
                       That clock STARTS once Phase 5 commits push
                       and the runner stops shipping "0 briefs /
                       quiet day". 5f confirmed the code path
                       works against the runner DB; the missing
                       inputs are deployment, not code.
                     * §8 changelog: Phase 5g entry added below.
                       Commit SHAs for 5a-5g remain pending push per
                       Mark's "Hold off until Phase 5 is done"
                       standing order. SHAs will be appended to
                       individual phase entries when push happens.
                   Push checklist (when Mark lifts the hold):
                     1. git push origin main — fires Phase 5a/c/d/e/f
                        in one go.
                     2. db.ensure_schema() runs on next workflow
                        invocation; ALTER TABLE adds
                        intel_first_brief_at on the runner DB
                        (idempotent, no-op locally).
                     3. Next scheduled daily-curator run reads via
                        today_curated_seeds() instead of
                        today_entries(); first day of non-empty
                        output starts the §5 Phase 4c trigger clock.
                     4. After 3 consecutive non-empty days, execute
                        §5 Phase 4c launchctl unload steps and
                        append timestamps + final inventory.json row
                        count to §8.
                   Note on Phase 1d historical import gap (5f finding):
                     The 1216-row historical co-source half of the
                     dual-source design is empty on the runner
                     because Phase 1d's import_inventory CLI was
                     run locally only (inventory.json lives in
                     ~/Library/Application Support/quantum_intel/,
                     which doesn't exist on GH Actions). Options:
                       (a) Accept today×today briefs as steady-state
                           (synth prompt allows this path; 5f
                           briefs were high quality).
                       (b) Check inventory.json into the repo (or a
                           release artifact) and run the importer
                           inside the daily-curator workflow on a
                           one-shot conditional ("if
                           quantum_intel_entries empty → import").
                     Decision deferred — neither blocks Phase 5
                     closure. Recommend (a) for now: the synth path
                     proven in 5f works at brief quality 0.58-0.72
                     confidence without historical context, and (b)
                     adds CI-side state + a 4.6 MB JSON checkin
                     that doesn't carry its weight unless the 1216
                     historical entries are demonstrably uplifting
                     today's briefs. Re-open after ≥ 1 week of
                     production output if quality plateaus.

              Out of scope for Phase 5 (deliberately):
                * Re-implementing Intel's DDG Lite collectors. Those
                  were heavily duplicated with Curator's RSS intake
                  and prone to CAPTCHA stalls. Drop them.
                * Re-implementing Intel's separate dedup table.
                  Curator's raw_articles_url_unique already covers
                  the same surface for new intake. quantum_intel_dedup
                  stays as the historical fingerprint set, never
                  written to again.
                * Backfilling quantum_intel_entries from new
                  curated_posts. Plan B explicitly keeps the two
                  tables separate by role (historical vs today's
                  seeds); no shim needed.

              Rollback (if Phase 5 regresses):
                Comment out intel-email + share-intel-summary in
                daily-curator.yml (same pattern as Twitter/X). The
                rest of Curator (publish, Bluesky per-article,
                insights email) is unaffected — Phase 5 only
                changes intel/*.py.

--------------------------------------------------------------------------------
6. LOCKED DECISIONS (do not re-litigate without changing this section)
--------------------------------------------------------------------------------

  D1. Schema model:               SQLite table in Curator (Mark answer Jun 9)
                                  NOT keeping inventory.json as canonical
  D2. APS journals:                PRX Quantum only — PRL too noisy
                                  (Mark: "whatever seems best")
  D3. NewsAPI:                     If key present in env, fires; if not,
                                  logged-and-continues. No change to
                                  default behavior.
  D4. Bluesky daily link target:   qrater.org root
                                  (Mark: "Quantum Crier daily summary is
                                   pretty long")
  D5. Curator email:               Add subvurs_impact_score section
                                  (Mark confirmed it was never added)
  D6. Intel email:                 Stays separate — own SMTP body
                                  (Mark: "either email is already full")
  D7. SCAFFOLD:                    Dropped entirely
                                  (Mark: "we stopped it a while back")
  D8. REVIEW:                      Dropped — it's not a Python stage,
                                  it's a separate claude CLI that
                                  frequently rc=1's anyway
  D9. Cutover shape:                Parallel-run, then disable Intel cron
  D10. Preservation:               All 1216 entries + 38 dedup sentinels
                                  + 9 scored entries' fields preserved
                                  exactly; entry_ids verbatim
  D11. Intel inventory.json:       Frozen as historical artifact post-cutover.
                                  Pre-import backup retained.
  D12. Synthesizer LLM:            Anthropic SDK direct via Curator's
                                  settings.claude_model; NO multi-provider
                                  fallback chain

--------------------------------------------------------------------------------
7. RISKS + MITIGATIONS
--------------------------------------------------------------------------------

  R1. Curator DB corruption during import.
      Mitigation: backup of inventory.json before any write; SQLite
      INSERT OR IGNORE idempotence; pre-import row counts captured
      for parity check (sources=18, raw_articles=448, curated_posts=28,
      etc.). Re-running the importer is a no-op. (Verified Jun 9.)

  R2. Curator publication path breakage.
      Mitigation: Phase 1 touched ONLY new tables and additive source
      entries. Existing tables unchanged. Existing publish path
      (publisher.py, site/, qrater_builder.py, email_report.py,
      bluesky.py) not touched in Phase 1.

  R3. Intel cron continues running and double-writes during parallel-run.
      Mitigation: Intel's cron writes to ITS OWN inventory.json — no
      shared writer. Curator-side synth reads quantum_intel_entries
      from Curator DB. The two pipelines are non-overlapping.

  R4. Anti-recurrence breakage in the new synthesizer.
      Mitigation: Phase 2 implementation preserves
      recent_brief_citations() semantics by reading from the new
      first_brief_at column on quantum_intel_entries. Golden-replay
      test against a small sample of Intel's existing briefs before
      cutover.

  R5. Goodhart on the daily summary LLM call.
      Mitigation: structured JSON output with explicit
      "no novel claims" constraint, fail-closed if response is not
      parseable JSON. Track wall-clock vs token count to detect
      slow-drift.

  R6. Schema drift between Intel inventory.json and the SQLite table.
      Mitigation: imported_from audit column records provenance;
      INSERT OR IGNORE means new fields in future Intel-format
      documents are silently dropped — log a warning if any.

  R7. (Phase 5) Output channel cross-contamination — Intel brief
      bodies leaking into Bluesky (300-char limit, gets truncated
      and looks broken) or into the Curator insights email
      (duplicates content).
      Mitigation: 5e smoke test in CI logs. Bluesky payload is
      built ONLY from daily_summary.render_bluesky(), which is
      explicitly short-form. Intel email payload is built ONLY
      from emailer.render_intel_email(). Curator insights email
      payload is built ONLY from email_report.build_insights
      _report() (reads curated_posts.subvurs_notes /
      subvurs_impact_score; never reads briefs). No shared
      string concatenation across these three.

  R8. (Phase 5) Goodhart on "non-empty for 3 days" cutover gate.
      If the synthesizer produces low-quality briefs to clear the
      gate, we cut Intel over to a worse pipeline.
      Mitigation: gate is "≥ 5 briefs/day with non-empty body
      AND at least one ≥ 0.3 subvurs_impact_score reference in
      either the seed or the paired historical entry" — not just
      brief count. Mark eyeballs the first 3 days' Intel emails
      before authorizing 4c unload.

--------------------------------------------------------------------------------
8. CHANGELOG
--------------------------------------------------------------------------------

  Jun 9 2026  Phase 0 SIGALRM patches landed in Intel repo
              (commit pending — see Phase 0 row)
  Jun 9 2026  Phase 1a — db.py schema additions landed
              (quantum_intel_entries, quantum_intel_dedup,
              bluesky_daily_summaries)
  Jun 9 2026  Phase 1b — BUILTIN_SOURCES additions landed
              (cond-mat, Quantinuum, PRX Quantum)
  Jun 9 2026  Phase 1c — importer module written
              (quantum_curator/intel/import_inventory.py)
  Jun 9 2026  Phase 1d — live import succeeded
              (1216 entries + 38 dedup sentinels + 9 scored,
               idempotence verified, max(entry_id) preserved)
  Jun 9 2026  Migration doc created (this file)
  Jun 9 2026  Phase 2 started
  Jun 9 2026  Phase 2 complete — intel/{inventory_view, brief_history,
              synthesizer, daily_summary, emailer}.py shipped; CLI
              commands synthesize-intel / intel-summary / intel-email
              registered; smoke-tested against live 1216-entry DB.
  Jun 9 2026  Phase 3 complete — bluesky.py share_daily_summary +
              record/is_daily_summary_shared helpers; email_report.py
              IMPACT_TABLE block (top-N, 0.05 floor); cli.py
              share-intel-summary command. Synthetic-post test
              verified floor + ranking + header count.
  Jun 9 2026  Phase 4a complete — daily-curator.yml now runs
              intel-email + share-intel-summary after Bluesky share
              and before the insights email. Both steps
              continue-on-error. YAML validated (20 named steps).
  Jun 9 2026  Phase 4b complete — parallel-run protocol documented
              (§5 Phase 4b). Both Intel launchd plists left loaded
              for the parity window; quantum_intel_entries stays
              static during the window per D11.
  Jun 9 2026  Phase 4c complete — Intel launchd retirement runbook
              documented (§5 Phase 4c). Actual `launchctl unload`
              execution deferred until parallel-run window passes;
              timestamp + final inventory.json row count to be
              appended to this changelog at that time.
  Jun 9 2026  First workflow_dispatch run (id 27193404135) exposed
              Intel content gap: intel-email rendered "0 briefs",
              share-intel-summary posted "quiet day", while Curator
              insights email simultaneously found 15 subvurs_notes
              connections. Root cause: synthesizer reads
              quantum_intel_entries.date_collected over last N
              days, but Phase 1d was a one-shot import — nothing
              populates new rows. Phase 5 plan landed below.
  Jun 9 2026  Phase 5 plan landed — Plan B (curated_posts as seeds,
              1216 quantum_intel_entries kept as combinatorial
              co-source). Explicit channel separation: Intel email =
              detailed review; Bluesky daily summary = short public
              rundown. Phase 4c retirement blocked on Phase 5
              producing ≥ 3 days non-empty output.
  Jun 9 2026  Phase 5a complete — inventory_view.py: SEED_ID_OFFSET +
              today_curated_seeds(days) filtering on curated_at (not
              published_at). synthesizer.py: run_intel_synthesis()
              pivoted to today_curated_seeds(); deliver() guards
              mark_first_brief_at against synthetic IDs. 1216
              historical entries unchanged in role (inventory side of
              cross-product, via load_inventory()). 5b subsumed.
              Caveat: local dev DB is months stale; 5f hand-validation
              must use the GH Actions runner cache, not the local DB.
  Jun 9 2026  Phase 5c complete — db.py: curated_posts gains
              intel_first_brief_at TEXT column + idempotent ALTER TABLE
              migration. inventory_view.py:
              mark_curated_seed_first_brief_at(uuid, ts). synthesizer.py:
              deliver() now takes seed_id_to_uuid map and routes
              citations (Intel-side int eid → quantum_intel_entries
              .first_brief_at; synthetic seed eid → curated_posts
              .intel_first_brief_at via UUID lookup). End-to-end mixed
              citation test verified. 28/28 existing rows initialized
              NULL. 11/11 pre-existing tests still pass.
  Jun 9 2026  Phase 5d complete — daily_summary.py pivoted to dual
              data source: defaults route today via today_curated
              _seeds(days=days) and prior via load_inventory()
              [:prior_limit]. Added kw-only ``days`` and ``prior
              _limit`` params; ``prior_days`` preserved as no-op for
              CLI backward-compat. SUMMARY_PROMPT and _no_new
              _content_payload reworded to "Prior corpus" / "No new
              curated posts to summarize" (contract keys, types,
              tags unchanged). FORBIDDEN_PHRASES list unchanged.
              Smoke test (mocked sources, mocked Anthropic key)
              confirms defaults routing + quiet-day payload. All
              three legacy CLI kwarg shapes still accepted. 11/11
              pre-existing tests still pass. Worth flagging: CLI
              call sites still feed today_entries (intel-side,
              frozen → empty in prod) as new_entries explicitly,
              bypassing the new default. Documented as a CLI gap
              under §5 5d; needs a 3-line patch per call site
              before 5e CI smoke test or 5f hand-run will succeed.
  Jun 9 2026  Phase 5e complete — CLI source pivot landed across
              four call sites (intel_summary, intel_email,
              share_intel_summary, synthesize_intel in cli.py) plus
              the emailer.py send_intel_email fallback; all now
              feed today_curated_seeds(days=...) instead of the
              frozen today_entries(). The two deliver() call sites
              (intel_email + synthesize_intel) build and pass the
              seed_id_to_uuid map so Phase 5c routing fires on
              seed-side citations. tests/test_intel_synth.py adds
              15 cases pinning: deliver() Intel-vs-seed routing,
              hallucinated-ID no-op, idempotence on both stamp
              paths, non-integer eid tolerance, today_curated_seeds
              projection shape (synthetic IDs, status/window
              filtering, field mapping), and channel-separation
              smoke (render_bluesky short+link-only, render_text
              multi-section, quiet-day payload renders sanely on
              both channels). Test suite: 26 passed (11 prior + 15
              new), no regressions. The CLI gap flagged under 5d
              is closed; 5f hand-run will now actually exercise
              the curated-seed path.
  Jun 9 2026  Phase 5f complete — hand-run validation against the
              Jun 9 09:41 UTC scheduled daily-curator run cache
              (artifact curator-db-27197625739). Local Phase 5e CLI
              ran end-to-end against scratch DATA_DIR pointed at the
              downloaded curator.db.
              Result: synthesize-intel --days 1 --max-briefs 5
              produced 5 non-trivial briefs (2.7-3.0 KB each, full
              Discoveries Combined / Target Market / Risk Factors
              structure) over 45 curated_posts in the 1d window.
              Confidence range 0.58-0.72. All 15 citations land in
              the SEED_ID_OFFSET range (>=2_000_000) — no
              cross-namespace collisions. 14 unique seeds cited (one
              collision: seed 2000002 cited by two briefs).
              Routing verification:
                * curated_posts.intel_first_brief_at stamped: 14 rows
                  (matches unique-seed count). All timestamps match
                  the synth run's UTC second.
                * quantum_intel_entries.first_brief_at touched: 0
                  rows (correct — seed IDs >= 2_000_000 don't route
                  to the Intel-side stamp path).
              Two deployment gaps discovered on the GH Actions runner
              cache that the migration doc had not previously flagged:
                * Phase 1d historical import never ran on the GH
                  runner — quantum_intel_entries has 0 rows there
                  (vs 1216 locally). Means the "prior corpus" half
                  of the dual-source design is empty in production;
                  briefs are today×today combinations (the synth
                  prompt's allowed-but-exception path), not
                  today×historical as the §5 5f spec assumed.
                  Quality stayed high — synthesizer handled empty
                  history via the "(No prior history — this is the
                  first run.)" fallback without degradation.
                * Phase 5c migration (intel_first_brief_at column)
                  never deployed — schema was missing on the runner
                  artifact. Applied locally via idempotent ALTER
                  TABLE before invoking the CLI; the same migration
                  will need to fire on the runner once Phase 5
                  commits are pushed (Phase 5c db.py
                  ensure_schema() already does this, so just
                  pushing closes the gap).
              Worth flagging: the deployed (pre-5e) GH workflow logs
              from the 09:41 UTC run showed "Window: 0 new entries
              (last 1d)" / "#quiet-day" — confirms the legacy
              today_entries() path is what production is still
              running. Phase 5 commits remain unpushed per Mark's
              "Hold off until Phase 5 is done" standing order; this
              is consistent.
              Validation artifacts: /tmp/phase5f_validation/data/
              intel_briefs/ (5 markdown files, retained for 5g
              writeup). DB at /tmp/phase5f_validation/data/
              curator.db (with 5c migration applied + 14 stamps
              written). Run timestamp: 2026-06-09T09:59:48 UTC.
  Jun 9 2026  Phase 5g complete — Phase 5 closed on docs side.
              §4 AFTER corrected: TODAY'S SEEDS filter is curated_at
              (not published_at), with the gotcha called out inline.
              The previous "where published_at ≥ now − 1d" reading
              would have been wrong in two ways: (a) older arXiv
              papers re-surfaced by today's intake would be silently
              excluded, and (b) the §5 5a implementation already
              uses curated_at — §4 had drifted from §5 between Plan
              A draft and Plan B pivot. §4 AFTER also clarifies
              quantum_intel_entries is frozen at Phase 1d (no shim
              accretion; 5b formally subsumed). §5 (5g) marked DONE
              with explicit push checklist:
                1. git push origin main fires 5a/c/d/e/f.
                2. db.ensure_schema() ALTER TABLEs
                   intel_first_brief_at on the runner (closes 5f gap).
                3. First non-empty daily-curator run starts the
                   §5 Phase 4c 3-consecutive-days clock.
                4. After 3 days non-empty, execute Phase 4c launchctl
                   unload + append the unload timestamp + final
                   inventory.json row count here.
              Phase 1d historical-import gap (5f finding): decision
              deferred between (a) accept today×today briefs as
              steady-state — 5f briefs were 0.58-0.72 confidence
              without historical context — or (b) check
              inventory.json into a release artifact and run the
              importer inside the workflow conditionally. Phase 5g
              recommends (a) for now; revisit after ≥ 1 week of
              production output if quality plateaus. Neither option
              blocks Phase 5 closure.
              Test suite: 26/26 (unchanged from 5e). No code changes
              in 5g — documentation only. Phase 5 complete; Phase
              4c execution waits on Mark lifting the push hold.

--------------------------------------------------------------------------------
9. ROLLBACK
--------------------------------------------------------------------------------

  If Phase 2+ goes wrong:
    1. Disable the new synthesize-intel CLI step in daily-curator.yml
    2. Re-enable Intel's launchd plist if it was disabled
    3. Inventory file is unchanged (we only ever read it); the live
       Intel cron resumes against it
    4. quantum_intel_entries / quantum_intel_dedup /
       bluesky_daily_summaries are inert without the new CLI step —
       leaving them in place harms nothing

  If Phase 1 import itself needs to be undone (unlikely):
       DROP TABLE quantum_intel_entries;
       DROP TABLE quantum_intel_dedup;
       DROP TABLE bluesky_daily_summaries;
    Then re-run init_db() to recreate the (now empty) tables, or
    restore from backup at inventory.json.preimport.bak.20260609_023717.

================================================================================
END OF DOCUMENT — update at the END of each phase, never mid-phase.
================================================================================
