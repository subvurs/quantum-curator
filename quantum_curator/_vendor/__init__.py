"""Vendored third-party packages.

Modules under this directory are copied verbatim from external sources
to make Quantum Curator self-contained at build/deploy time. See
each subpackage's VENDORED.md for provenance.

DO NOT import this package directly — import the vendored subpackage:

    from quantum_curator._vendor.subvurs_impact import score_item
"""
